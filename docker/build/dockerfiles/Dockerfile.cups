FROM debian:trixie

RUN apt-get update && \
    apt-get install -y cups cups-client cups-filters printer-driver-all && \
    rm -rf /var/lib/apt/lists/*

COPY printers.conf /etc/cups/printers.conf
COPY cupsd.conf /etc/cups/cupsd.conf
COPY ppd/ /etc/cups/ppd/

EXPOSE 631

CMD ["cupsd", "-f"]
