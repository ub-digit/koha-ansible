export_dir="${PRIMO_EXPORT_EXPORT_DIR}"

tar_ext=".tar.gz"
tar_opts="czf"

file_prefix="46GUB-delta-"
file_ext=".marc"

primo_user="${PRIMO_EXPORT_UID}"
primo_group="${PRIMO_EXPORT_UID}"

exp_tmp="$$"
exp_root="/var/tmp"
exp_dir="$exp_root/$exp_tmp"

koha_path="${KOHA_REPO_PATH}"
koha_instance="${KOHA_INSTANCE_NAME}"
koha_shell="/usr/sbin/koha-shell" # TODO: Change to cusom koha-shell with correct exit signal handling?
export_records_opts="--dont_export_items --deleted_marc_conditions=942n=1"

data_dir="${PRIMO_EXPORT_DATA_DIR}"
