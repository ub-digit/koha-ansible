base_dir: {{ [edifact_repo_path, 'vendors'] | path_join }}
email: gunda@ub.gu.se

vendors:
  test:
    host: sftp
    protocol: sftp
    port: '22'
    server_dir: './edifact-uploads'
    user: edifact
    pass: edifact
