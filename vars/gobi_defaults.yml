---
gobi_ftp_password: "{{ vault_gobi_ftp_password }}"
gobi_koha_user: "{{ koha_user }}"
gobi_koha_path: "{{ koha_repo_path }}"
gobi_koha_instance: "{{ koha_instance_name }}"

gobi_smtp_host: "{{ smtp_host }}"
gobi_smtp_port: "{{ smtp_port }}"

loadgobi_conf_file_path: "{{ data_dirs['gobi'].path }}/loadgobi.sh.conf"
gobi_log4perl_conf_file_path: "{{ data_dirs['gobi'].path }}/log4perl.conf"

gobi_report_mail_to: david.gustafsson@ub.gu.se,g.katalog@ub.gu.se
gobi_error_mail_to: david.gustafsson@ub.gu.se,g.katalog@ub.gu.se,stefan.berndtsson@ub.gu.se

